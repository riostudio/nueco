[pytest]
# Scope to the simulation eval suites only (repo root has legacy scripts that
# would otherwise be collected and hit live infrastructure).
testpaths = tests/evals
python_files = *_benchmarks.py test_*.py
python_functions = test_*
asyncio_mode = auto
addopts = -ra -q
